import React from 'react'
import InputLabel from '@mui/material/InputLabel';

function InputLabelField({value, ...rest}) {
  return (
    <InputLabel>{value}</InputLabel>
  )
}

export default InputLabelField